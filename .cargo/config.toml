# Portable defaults only. Point cargo at your UCX install via env:
#   export UCX_PREFIX=/path/to/ucx
#   export LD_LIBRARY_PATH=$UCX_PREFIX/lib${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}
# See .cargo/config.toml.example for optional local overrides.

[target.x86_64-unknown-linux-gnu]
rustflags = ["-C", "target-cpu=native"]
