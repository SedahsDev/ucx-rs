# Prefer:
#   export UCX_PREFIX=/path/to/ucx
#   export LD_LIBRARY_PATH=$UCX_PREFIX/lib:$LD_LIBRARY_PATH

[target.x86_64-unknown-linux-gnu]
rustflags = [
    "-C", "target-cpu=native",
    # "-L", "/path/to/ucx/lib",
    # "-C", "link-arg=-Wl,-rpath,/path/to/ucx/lib",
]
